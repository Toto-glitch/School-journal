<Page x:Class="SchoolJournal.View.Pages.SubjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Предметы">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Список предметов" FontSize="24" FontWeight="Bold"/>
            <Button Content="+ Добавить" HorizontalAlignment="Right" Padding="15,8"
                    Command="{Binding AddSubjectCommand}"
                    Visibility="{Binding IsDirector, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}"
                  AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Классов" Binding="{Binding Classes.Count}" Width="80"/>
                <DataGridTextColumn Header="Учителей" Binding="{Binding Teachers.Count}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0"
                    Visibility="{Binding IsDirector, Converter={StaticResource BoolToVisibilityConverter}}">
            <Button Content="Редактировать" Command="{Binding EditSubjectCommand}" Margin="0,0,10,0"/>
            <Button Content="Удалить" Command="{Binding DeleteSubjectCommand}" Background="#FFE0E0"/>
        </StackPanel>

        <!-- Диалог добавления/редактирования -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#80000000"
                Visibility="{Binding IsDialogOpen, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="White" CornerRadius="8" Padding="20" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="300">
                <StackPanel>
                    <TextBlock Text="{Binding IsEditing, Converter={StaticResource BoolToStringConverter}, 
                               ConverterParameter='Редактирование предмета|Новый предмет'}" 
                               FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <TextBlock Text="Название" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Отмена" Command="{Binding CancelCommand}" Margin="0,0,10,0"/>
                        <Button Content="Сохранить" Background="#4CAF50" Foreground="White"
                                Command="{Binding SaveCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Page>